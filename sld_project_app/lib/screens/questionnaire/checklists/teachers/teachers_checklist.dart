import 'package:flutter/material.dart';

class TeachersChecklist extends StatelessWidget {
  const TeachersChecklist({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Teachers checklist starts here"),
      ),
    );
  }
}
